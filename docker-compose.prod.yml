services:
  backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile.prod
    volumes:
      - static_volume:/app/itemyze/staticfiles
      - media_volume:/app/itemyze/mediafiles
      - db_prod:/app/db_data
    env_file:
      - ./.env.prod
    restart: always
    command: gunicorn --bind 0.0.0.0:8000 itemyze.wsgi:application
    working_dir: /app/itemyze
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8000/admin/login/']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  frontend:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile.prod
    volumes:
      - frontend_static:/app/build

  nginx:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
    ports:
      - '80:80'
    volumes:
      - static_volume:/var/www/staticfiles
      - media_volume:/var/www/mediafiles
      - frontend_static:/var/www/frontend
    restart: always
    depends_on:
      - backend
      - frontend
    env_file:
      - ./.env.prod

volumes:
  static_volume:
  media_volume:
  frontend_static:
  db_prod:
